#include "user/user.h"
#include "kernel/param.h"

int
main(int argc, char *argv[])
{
    // if(argc == 1) {
    //     fprintf(2, "xargs: no command given\n");
    //     exit(1);
    // }

    // char *cmd = argv[1];
    // char *args[MAXARG];
    // for (int i = 1; i < argc; i++) {
    //     args[i - 1] = argv[i];
    // }

    // args[argc - 1] = 0;

    // char c;
    // char buf[512];
    // while(read(0, &c, 1) == 1) {

    // }
}